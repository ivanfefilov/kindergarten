$("#comments").prepend("<%= j render partial: 'comment', locals: {comment: @comment} %>")
$('h3[data-comments]').text("<%= t('misc.comments', count: @post.comments.count) %>")
$('.post-date span a').text("<%= t('misc.comments', count: @post.comments.count) %>")
$('textarea[name="body"]').val('')
$('#comments').prepend("<%= j render partial: 'shared/flash_messages' %>")
$('html,body').animate({scrollTop: $('h3[data-comments]').offset().top});
